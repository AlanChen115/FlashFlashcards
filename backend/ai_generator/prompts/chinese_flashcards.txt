Respond ONLY with valid JSON. Do NOT include commentary.

Extract ALL vocabulary relevant to learners, including:
- verbs
- nouns
- adjectives
- adverbs
- important proper nouns (names of people, places, organizations)

Each item must include the following common fields:

{
  "word": "string",
  "lemma": "string",
  "pinyin": "string",
  "translation": "string",
  "example_sentence": "string",
  "translated_example_sentence": "string",
  "type": "verb" | "noun" | "adjective" | "adverb" | "proper noun"
}

Lemma should be the base or canonical form of the word.

IMPORTANT:
- The example_sentence MUST be an ORIGINAL sentence CREATED BY YOU.
- Do NOT copy or reuse sentences or phrases from the article.
- The example_sentence must be simple, natural, and appropriate for an HSK 1–4 learner.

Additional fields by part of speech:

For NOUNS include:
{
  "measure_word": "个"
}

Everything else should just be the common fields